name: Jules Assessment Generator (Worker)

on:
  workflow_call:
  workflow_dispatch:
  # NOTE: No schedule trigger - called by Control Tower to avoid duplicate runs

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install
        run: npm install -g @google/jules

      - name: Archive
        run: |
          mkdir -p docs/assessments/archive
          shopt -s nullglob
          for f in docs/assessments/Assessment_*.md; do mv "$f" docs/assessments/archive/; done
          shopt -u nullglob

      - name: Generate
        env:
          JULES_TOKEN: ${{ secrets.JULES_API_KEY }}
        run: |
          jules auth --token $JULES_TOKEN
          jules task fix --prompt "Generate graded assessments (0-10) for categories A-O. Save to docs/assessments/. Issues for grades < 5."

      - name: Commit
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs(assessment): weekly"
          file_pattern: "docs/assessments/**/*.md"
