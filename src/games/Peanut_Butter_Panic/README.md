# Peanut Butter Panic

**Peanut Butter Panic** is a Python-based game structured as a complete package.

## Features

- **Package-Based Structure**: Designed to be run as a module.
- **Gameplay**: (Add gameplay details here once verified, or keep generic if unknown)

## How to Run

From the root of the repository:

```bash
python game_launcher.py
```
Select "Peanut Butter Panic" from the launcher.

Alternatively, run directly from the terminal (ensure you are in the `games` directory or set PYTHONPATH):

```bash
# Run as a module
cd games
python -m Peanut_Butter_Panic.peanut_butter_panic.game
# OR if configured with __main__
python -m peanut_butter_panic.game
```

*(Note: Verify the exact module path in `game_launcher.py`: `peanut_butter_panic.game`)*

## Development

- **Source Code**: `games/Peanut_Butter_Panic/peanut_butter_panic/`
- **Tests**: `games/Peanut_Butter_Panic/tests/`
